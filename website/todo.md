## Notes for the web publishing team

These items don't belong in the page body above but should be tracked when this content goes live:

- Introduce automated weekly link checking in the website publication pipeline.
- Create `/domains/registers/era-knowledge-graph_en` and permanently redirect the misspelled URL.
- Do not publish query URLs containing hundreds of encoded characters. Store each query under a stable identifier and construct execution requests inside the Data Stories application (reflected above under Featured Data Stories).
